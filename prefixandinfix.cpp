// pseudo-code for evaluatin of infix to postfix

// we assume exp is correct and there no edge cases

// exp consists of tokens - operator and operand

// only operands are pushed in stack

char[51] exp;

void calculate(operand, operand, operator){
    return res;}

void EvalutePostfix(exp){
    
    stack s;
    
    for(int i=0;i<n-1;i++)
    if (exp[i]== operand){
        
        s.push(exp[i];
        
        }
        elif(exp[i] is operator){
            
            a1=s.pop();
            a2=s.pop();
            
            res=calculate(a1,a2,exp[i]);
            s.push(res);
            }
    
    return top_stack();
    
    }



int main(){
    
    
    
    
    }
